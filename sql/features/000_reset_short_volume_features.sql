-- Short-interest cycles depend on these features, so they must be rebuilt too.
DROP TABLE IF EXISTS market_structure.short_interest_cycles;
DROP TABLE IF EXISTS market_structure.short_volume_features;
